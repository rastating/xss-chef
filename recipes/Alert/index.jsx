import { cook, init, render, validate } from './Alert'

export default {
  title: 'Alert',
  description: `Display an alert in the user's browser`,
  cook: cook,
  validate: validate,
  init: init,
  render: render
}
