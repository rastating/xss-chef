import { cook, render, validate, init } from './DecimalEncoder'

export default {
  title: 'Decimal Encoder',
  description: 'Encode the payload into a comma separated array of decimal numbers',
  cook: cook,
  render: render,
  validate: validate,
  init: init
}
